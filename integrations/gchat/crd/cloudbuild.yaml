steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "$_IMAGE_URI",
        "-f",
        "agents/platform/Dockerfile",
        "--build-arg",
        "HERMES_AGENT_TAG=$_HERMES_AGENT_TAG",
        ".",
      ]
images:
  - "$_IMAGE_URI"
