--- a/plugins/platforms/google_chat/adapter.py
+++ b/plugins/platforms/google_chat/adapter.py
@@ -504,6 +504,8 @@
         self._typing_messages: Dict[str, str] = {}
         self._shutting_down = False
         self._rate_limit_hits: Dict[str, int] = {}
+        self._status_message_ids: Dict[Tuple[str, str, str], str] = {}
+        self._status_lock: Optional[asyncio.Lock] = None
         # Last-seen inbound thread name per chat_id (space). Google Chat
         # DMs create a NEW thread per top-level user message but the user
         # views them as one logical conversation. We:
@@ -1870,6 +1872,40 @@
         finally:
             self.resume_typing_for_chat(chat_id)
 
+    async def send_or_update_status(
+        self,
+        chat_id: str,
+        status_key: str,
+        content: str,
+        *,
+        metadata: Optional[Dict[str, Any]] = None,
+    ) -> SendResult:
+        """Send or update a status message in-place for Google Chat."""
+        thread_id = ""
+        if metadata:
+            thread_id = metadata.get("thread_name") or metadata.get("thread_id") or ""
+        key = (str(chat_id), str(thread_id), str(status_key))
+        
+        if getattr(self, "_status_lock", None) is None:
+            self._status_lock = asyncio.Lock()
+            
+        async with self._status_lock:
+            cached_id = self._status_message_ids.get(key)
+            if cached_id is not None:
+                result = await self.edit_message(
+                    chat_id, cached_id, content, finalize=True
+                )
+                if result.success:
+                    if result.message_id:
+                        self._status_message_ids[key] = str(result.message_id)
+                    return result
+                # Edit failed (e.g. message deleted), fall through to fresh send.
+                self._status_message_ids.pop(key, None)
+            result = await self.send(chat_id, content, metadata=metadata)
+            if result.success and result.message_id:
+                self._status_message_ids[key] = str(result.message_id)
+            return result
+
     async def edit_message(
         self,
         chat_id: str,
