name: Staging Redeploy Agent

on:
  push:
    tags:
      - 'staging/**'

concurrency:
  group: staging-redeploy-agent
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  call-deploy:
    # DBG TEST: Commenting out repo guard so it runs on fork
    # if: github.repository == 'gke-labs/kube-agents'
    uses: ./.github/workflows/reusable-deploy-agent.yml
    with:
      # DBG TEST: Hardcoded values for testing
      # target_environment: 'staging'
      # agent_tag: ${{ github.sha }}
      target_environment: 'kkosov-dev'
      agent_tag: '193c92e45ca2159df10958f6937f8c6154c855a4'
      checkout_ref: ${{ github.ref }}
